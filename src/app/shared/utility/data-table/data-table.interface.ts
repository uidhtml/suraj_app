export interface DataTableElements {
  edit?: string;
  delete?: string;
  id: number;
  name: string;
  category: string;
  image?: string;
  cutoutPrice: number;
  price: number;
  unit: string;
  stock: number | string;
}
